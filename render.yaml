services:
  - type: web
    name: mock-generator
    runtime: python
    buildCommand: "pip install -r requirements.txt && apt-get install -y ffmpeg"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
